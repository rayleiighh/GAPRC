# GAPRC Backend - Configuration locale

# Base de donnees
DB_HOST=localhost
DB_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=gaprc

# Securite
JWT_SECRET=change_me_strong_secret

# Lien frontend (emails de reset)
FRONTEND_URL=http://localhost:4173

# Email (reset password)
EMAIL_USER=your_email@example.com
EMAIL_PASS=your_app_password

# Optionnel (phase materiel - a activer plus tard)
# KIOSK_API_KEY=change_me_kiosk_key
